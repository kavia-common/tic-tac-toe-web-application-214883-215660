# PostgreSQL connection settings for the backend container
# Either provide POSTGRES_URL (full SQLAlchemy URL), or set individual fields below.

# Full connection URL (takes precedence if set)
# Example: postgresql+psycopg://user:password@hostname:5432/database
POSTGRES_URL=

# Individual fields (used only if POSTGRES_URL is not provided)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=tic_tac_toe_database
POSTGRES_PORT=5432
POSTGRES_DB=postgres
